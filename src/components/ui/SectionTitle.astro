---
const {title, description} = Astro.props;
---

<div class="section-header text-center space-y-4 mb-4 lg:mb-8">
    <h2 class="font-accent font-bold text-heading-text lg:text-l-text">{title}</h2>
    <p class="lg:w-[60%] lg:mx-auto">{description}</p>
</div>
